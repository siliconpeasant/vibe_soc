# opentitan_common

This package is kept as an OpenTitan compatibility bucket for shared files that do not have a better native vibe_soc IP owner yet.

`prim` and `prim_generic` sources are intentionally not owned here. They are owned by `ip/digital/opentitan_prim` to avoid duplicate source ownership between `common` and `prim`.
